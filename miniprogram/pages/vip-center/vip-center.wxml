<view class="page">
  <view class="hero-card">
    <text class="hero-title">{{statusTitle}}</text>
    <text class="hero-desc">{{expireText}}</text>
    <view class="hero-tags" wx:if="{{entitlements}}">
      <text class="hero-tag">{{entitlements.is_vip ? '任务数不限' : '最多' + entitlements.limits.max_active_tasks + '个未结束任务'}}</text>
      <text class="hero-tag">{{entitlements.is_vip ? '收集人数不限' : '单任务最多' + entitlements.limits.max_submissions_per_task + '人'}}</text>
      <text class="hero-tag">开放时间最多{{entitlements.limits.max_open_duration_days}}天</text>
      <text class="hero-tag">{{entitlements.limits.can_use_ai_analysis ? '支持AI分析' : 'AI分析仅VIP可用'}}</text>
    </view>
  </view>

  <view class="card">
    <text class="card-title">激活VIP</text>
    <text class="card-text">输入激活码后即可升级为 VIP，权益会立即生效。</text>
    <input
      class="input"
      placeholder="请输入激活码"
      value="{{redeemCode}}"
      bindinput="onRedeemInput"
      adjust-position="{{false}}"
    />
    <view class="primary-btn" bindtap="redeemCode">立即兑换</view>
  </view>

  <view class="card">
    <text class="card-title">VIP权益</text>
    <text class="benefit-item">1. 普通用户开放时间最多 7 天，VIP 最多 30 天</text>
    <text class="benefit-item">2. 进行中任务数量不限</text>
    <text class="benefit-item">3. 单任务收集人数不限</text>
    <text class="benefit-item">4. 导出完成后，普通用户可在 7 天内重新生成下载链接，VIP 可在 30 天内重新生成下载链接</text>
    <text class="benefit-item">5. 可开启 AI 分析</text>
    <text class="benefit-tip">VIP 到期后按普通用户规则生效，已有任务和提交记录不会删除。</text>
  </view>

  <view class="card">
    <text class="card-title">联系客服</text>
    <text class="card-text">如需帮助，可通过小程序内客服联系我们。</text>
    <view class="action-group">
      <button class="action-btn action-btn-primary" open-type="contact" session-from="vip_center_help">联系客服</button>
    </view>
  </view>
</view>
